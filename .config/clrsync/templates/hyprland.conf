-- Generated by clrsync

local primary        = "rgb({accent.hex_stripped})"
local surface        = "rgb({surface.hex_stripped})"
local secondary      = "rgb({accent_secondary.hex_stripped})"
local error_color    = "rgb({error.hex_stripped})"
local tertiary       = "rgb({base06.hex_stripped})"
local surface_lowest = "rgb({background.hex_stripped})"
local border         = "rgb({border.hex_stripped})"
local border_focused = "rgb({border_focused.hex_stripped})"

hl.config({
    general = {
        col = {
            active_border   = border_focused,
            inactive_border = border,
        },
    },

    group = {
        col = {
            border_active          = border_focused,
            border_inactive        = border,
            border_locked_active   = error_color,
            border_locked_inactive = surface,
        },

        groupbar = {
            col = {
                active          = secondary,
                inactive        = surface,
                locked_active   = error_color,
                locked_inactive = surface,
            },
        },
    },
})
