# Generated by clrsync

[global]
    frame_color = "{border.hex}"
    separator_color = frame
    highlight = "{accent.hex}"

[urgency_low]
    background = "{background.hex}"
    foreground = "{editor_comment.hex}"
    frame_color = "{border.hex}"

[urgency_normal]
    background = "{background.hex}"
    foreground = "{foreground.hex}"
    frame_color = "{border.hex}"

[urgency_critical]
    background = "{background.hex}"
    foreground = "{foreground.hex}"
    frame_color = "{error.hex}"
