// Generated by clrsync

layout {
    focus-ring {
        active-color   "{accent.hex}"
        inactive-color "{border.hex}"
        urgent-color   "{error.hex}"
    }

    border {
        active-color   "{accent.hex}"
        inactive-color "{border.hex}"
        urgent-color   "{error.hex}"
    }

    tab-indicator {
        active-color   "{accent.hex}"
        inactive-color "{border.hex}"
        urgent-color   "{error.hex}"
    }

    insert-hint {
        color "{accent.hex}80"
    }
}
