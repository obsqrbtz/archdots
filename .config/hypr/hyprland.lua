-- The order matches the previous source order in hyprland.conf.
-- clrsync overrides border colors set in general, so it MUST come after it.

require("hyprland.env")
require("hyprland.autostart")
require("hyprland.general")
require("hyprland.rules")
require("hyprland.clrsync")
require("hyprland.keybinds")
require("hyprland.monitors")
